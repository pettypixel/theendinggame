import React from 'react';
import { Button } from 'react-bootstrap';

function No () {
    return(
        <div>
            <Button> NO I WANT TO STAY </Button>
        </div>
    );
}

export default No;